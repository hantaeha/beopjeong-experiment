name: Keep Streamlit Apps Alive

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit Apps
        run: |
          curl -sS https://experiment-app-a.streamlit.app/ > /dev/null
          curl -sS https://experiment-app-b.streamlit.app/ > /dev/null
